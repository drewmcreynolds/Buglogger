{
  "name": "buglog",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd Buglog.client && npm i",
    "setup:server": "cd Buglog && npm i",
    "start": "node Buglog/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
